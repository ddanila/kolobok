# Repository instructions

## Git workflow

- This is a personal pet project. When the user explicitly asks to commit and
  push, commit and push directly to `main`.
- Do not create feature branches or pull requests unless the user explicitly
  requests one.
